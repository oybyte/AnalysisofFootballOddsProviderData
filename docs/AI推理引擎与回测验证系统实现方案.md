# AI 推理引擎与回测验证系统实现方案

文档状态：待实施设计（2026-08-06 拆分版）。当前正式规则为 `football-analysis@1.8.0`（Contract 7），活动实验为 `1.7.0 revision 1`。

本文仅作为新文档集的入口与迁移说明。它不构成 AI 配置激活、规则发布、正式锁定或 `agent sync` 授权。

请从 [AI 推理与回测验证系统](AI推理与回测验证系统/README.md) 开始阅读。该目录将通用契约、Phase 0-5 与实施验收分别维护，避免大文件中的实施与治理约束漂移。

## 固定边界

- AI 产物始终是与正式轨隔离的研究产物，不写入正式 Outlook、锁定、结算、复盘或正式统计。
- 实施前先完成确定性回放和时间泄漏验证，再开放任何真实 LLM 调用。
- 未获得 lcz 单独批准，不激活真实数据 AI 配置、不连接外部 provider、不发布规则。
