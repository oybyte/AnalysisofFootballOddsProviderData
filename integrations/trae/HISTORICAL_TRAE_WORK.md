# TRAE Work 历史说明

TRAE Work 已不再是本仓库的现役桌面产品。该目录只保留历史说明；历史认证记录继续可读，但不会被 `agent doctor`、`agent sync` 或当前认证汇总消费。

现役迁移目标为 `integrations/trae-cn/PROJECT_INSTRUCTIONS.md`。Trae CN 必须先完成真实客户端载入验证，之后才允许同步受管项目指令文件。
