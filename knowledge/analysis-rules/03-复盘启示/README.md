# 03-复盘启示

本目录存放**对已完成比赛复盘后总结出的规律、教训、修正项**。

它回答的是"过去的分析哪里对了、哪里错了、以后怎么改"，用于持续迭代
`01-分析架构` 与 `02-基础规则`。复盘结论若推翻旧规则，应在对应文件
将 `status` 改为 `已废弃` 并保留记录，同时在 INDEX.md 登记表同步。

## 本目录当前规则
（新增规则文件后，可在此列出文件名与一句话说明，便于速览）

- 暂无

## 录入指引
- 文件命名：`NN-主题.md`
- 遵循 `00-规则模板.md` 的 frontmatter 规范
- 录入后请在根目录 `INDEX.md` 的登记表中追加一行
