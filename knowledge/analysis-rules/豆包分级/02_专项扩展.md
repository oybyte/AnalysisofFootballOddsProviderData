# 【模块02｜专项扩展模块】
> 模块说明：联赛/场景专属规则（如韩K、芬超、冷门逆向阻盘等），须在底层必选模块加载完成后导入，叠加补充，不覆盖底层逻辑。
> 本文件为聚合子文档，完整内嵌本模块全部规则，无外部文件依赖。
> 加载完成后，模型须回复【模块02加载完成】，等待下一模块。

## 📋本模块目录
（当前暂无专项扩展规则；待补充各联赛/场景专属判定规则后，将按本模块格式追加）

============================================
# 模块结束

> ⚠️ 完整性校验：本模块当前为空，上传后模型回复【模块02加载完成】即视为正常。