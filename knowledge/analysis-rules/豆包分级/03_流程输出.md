# 【模块03｜流程输出模块】
> 模块说明：执行步骤、复盘规范、输出格式、校验指令（如赛前5步排查、复盘流程、输出表格格式），须在底层+专项模块加载完成后最后导入。
> 本文件为聚合子文档，完整内嵌本模块全部规则，无外部文件依赖。
> 加载完成后，模型须回复【全部规则模块加载完成，可以接收比赛数据】。

## 📋本模块目录
（当前暂无流程/输出类规则；待补充赛前排查、复盘规范、输出格式后，将按本模块格式追加）

============================================
# 模块结束

> ⚠️ 完整性校验：本模块当前为空，上传后模型回复【全部规则模块加载完成，可以接收比赛数据】即视为正常。